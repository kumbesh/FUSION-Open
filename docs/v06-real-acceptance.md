# Fusion v0.6 controlled real-lab acceptance

Status: **controlled real-lab scenarios PASS on 2026-09-13 UTC**. The dated
record below contains only telemetry generated by the Windows endpoint, Linux
endpoint, and Suricata sensor with empty synthetic `validation_id` markers.
Automated fixtures are not represented as real evidence.

This result closes the controlled real-lab scenario gate; it does not by itself
declare v0.6 ready for release. Fresh-install, upgrade, full regression,
performance, review, and CI gates remain separate.

This procedure exercises the four correlation scenarios frozen in
[`v06-correlation-model.md`](v06-correlation-model.md). It does not authorize a
public target, password guessing, a wordlist, an exploit, or permanent changes
to a sensor. Run it only in an isolated lab you own or are authorized to test.

## Recorded results — 2026-09-13 UTC

### Environment and result boundaries

- Result: **PASS** for all four initial correlation scenarios, incident
  lifecycle behavior, correlation-engine restart/deduplication, exact backlog
  drain, dashboard inspection, and temporary-lab cleanup described below.
- Branch: `codex/v0.6-correlation-engine`; repository HEAD at test time:
  `f0c1bf92413ba9c389e5eb8de75bf0473f087146`. The v0.6 candidate was tested
  from the local implementation worktree; this is not a release commit or tag.
- Compose project: `fusion-v06-step2-real-20260913` with five healthy services:
  ClickHouse, Vector, Fusion Detection Engine, Fusion Correlation Engine, and
  Grafana.
- Container image IDs at final verification:
  - ClickHouse:
    `sha256:fd037d424da807c1b36517f3ff6c739286c60a34c928279c229fc1faa0cadaa8`
  - Vector:
    `sha256:5dcf67db0ee378caa87f3395cb9484ebe3e97bb0334d119f2ac33116e00c5773`
  - Detection engine:
    `sha256:c2676e270a7c0e317f6b6ea85a5fa438b502c47f69dbafb9609be6da3cc57c4c`
  - Correlation engine:
    `sha256:a262ab6e23eb150c86fbc6be893d3e4d8354b3bd775b2ce3c8e68dfa1d5d71fa`
  - Grafana:
    `sha256:f772d434e8fab0049deb2b1b30abd43342bcfca1537614aa8d36080232cf4283`
- Lab topology: Windows host `Kumbesh-Desk` on VMware VMnet8, Fusion receiver
  `192.168.186.1`, and Ubuntu/Suricata host `fusion-ubuntu` at
  `192.168.186.129`. Scenario C used the temporary Windows alias
  `192.168.186.10` solely to obtain an unambiguous ownership fact.
- SQL evidence came from `fusion.incidents_current` and
  `fusion.incident_timeline`. Every accepted incident, detection, event, and
  link had `validation_id = ''`.
- No raw event JSON, command-line payload, password, or unrelated user data is
  retained in this document.

Required negative controls that could not be generated safely as real lab
telemetry were exercised by the strict shipped correlation fixture and
regression suite. The full correlation/Grafana test run completed with
`231 passed, 7 skipped`; those automated controls are not presented as real
telemetry or substituted for any positive real-lab result below.

### A. SSH failures followed by a successful login — PASS

Five controlled failed SSH authentications and one successful login were
generated for disposable user `fusion-v06-accept` on `fusion-ubuntu`, all from
`192.168.186.1`. The occurrence interval was
`2026-09-13 06:17:23.893`–`06:19:18.098` UTC (114.205 seconds), and the five
failures strictly preceded the success.

The five `fusion-linux-authentication-failure` detection/source/link identities
were:

| Detection ID | Source event UID | Link ID |
| --- | --- | --- |
| `677f7f0e717a01b870f16dce4dc1f5c74d6ec7b7a6c1a023bca3a482e757b0b2` | `c3c9958b28216092bdab7a38fddd640fc9192336de8ff378347a4d2404d9c3f8` | `66725d0925cf6caa0bfb65abb3829ebd033a03177de3ac2bab08533dcb2428c3` |
| `a0a6da9c36b0d823a546bdcd93f90cc11caf10f1ba7b680fb995bb70cd1be41d` | `d0040883bae67c30819b97a48337fca9ad6533092c60aa8c4f4c9021e383e0e9` | `b07fcc95550012cafbb619f1bdea8f46d7734de5ead4a5ff6ae39a41199cc8ce` |
| `66a18d5b429b51a2dcadc5e20b357e114cd208e883952a66d07ce37fddf87760` | `57edc4593c52dc244c5fb30eb8f1748d424310a94fe134ca0278cd328c53e499` | `7c9bf593c2a0c673797d9d25d56907208a3f56179172a61783c2fcf30c6ff3f3` |
| `3fce192161f2efea3220b10271fd51f68631e6f0b5f04ef5eee9a2561e37997e` | `d36d8de47866dd5451b877f4ac41d2a5904686da3f3607471f6a26eb716ec78a` | `c884ca8918ee9a8ffa4d81e92b2d38cc17ed7655c05ce7ff08a7ba2c0b25b36e` |
| `31cea35393bcb89d6856e28c45f3ebfc617f65ade8bfd9106d6feada23cb60dc` | `5121db2a768a7e73c83aeafd9a7bbd947f42f231d6b7a86a212dbd8dd10eb014` | `f9131a8f496c1649d88c31f25990492dd826625573af97acf7a500902bdfc0d6` |

The successful-login event UID was
`87b97391003f17cffe94e66dadee91df5c4a83128fee2176f8f139ef6f811a86`;
its trigger link was
`6e1a61973fe404685c60750231aa1bf4592d5ff26f389989f5647c2786689ea8`.

The resulting incident was:

- incident ID:
  `d8dff940738739ce71316c936ea376850abda737f697acd913c698dca0ea76f5`;
- rule: `fusion-correlation-ssh-bruteforce-success` v1; scope fingerprint:
  `5ff0910b77649598c2fcafadb603018bca065f66a17cf76bfe38cd27c28c42c8`;
- status/severity/confidence: `new` / `high` / `90`;
- MITRE ATT&CK: `TA0006` / `T1110`;
- exact membership: six inputs, five detections, and one event;
- immutable anchor: successful-login event
  `87b97391003f17cffe94e66dadee91df5c4a83128fee2176f8f139ef6f811a86`;
- episode window: `06:17:23.893`–`06:22:23.893` UTC; late-accept
  boundary: `06:37:23.893` UTC; and
- state hash:
  `3beab5e29a86f6e281e3e793e63a394e2a0d585a683beccbad345513f91f5f20`.

After correlation-engine restart, the incident ID, state hash, six-member set,
and link identities were unchanged; no logical incident, link, or ledger
duplicate appeared. The disposable account, home directory, test key, related
test processes, and lockout state were removed afterward without changing sshd,
PAM, or firewall policy.

### B. Two distinct suspicious detections on one Windows host — PASS

The real Windows inputs were:

- encoded PowerShell: detection
  `50427f70fb5ab91c5b0bb6a9e9544394373a0b7d334c5f004c1c5a3a815172af`,
  source event
  `8a879f4412b6cd0bb9168bcd0a6595cfd550884ddb7a85f770814becaf78f2c7`,
  occurrence `06:16:08.709` UTC, high, `T1059.001`; and
- harmless suspicious command-shell shape: detection
  `c5d83d3f28a90469e9f5048b4e4223f7ee91bef3644a26a882b63c908ea948af`,
  source event
  `6ab3bc219e2596b84ea5cb5b69ce1939a324535ae06d9443829fb7a1e31dac8f`,
  occurrence `06:19:00.832` UTC, medium, `T1059`.

Both inputs had raw host `Kumbesh-Desk`, raw user `KUMBESH-DESK\user`, and
different detection-rule IDs. The command-shell action was a `cmd.exe` comment;
it performed no file or network operation.

Incident
`ce807f44120dce3af3f80b027d0cee7c447374ef61a9a34a30040735a9c8ced4`
was created by `fusion-correlation-host-suspicious-activity` v1 with scope
fingerprint
`b97f40a5bd0362e2e7286aee7abe79e7259684fe0edb9bfe38d2d88d3901868e`.
Its initial state was `new`, high severity, confidence 70, canonical host
`kumbesh-desk`, exactly two detection members from two distinct child rules,
and no event members. Its anchor was detection
`c5d83d3f28a90469e9f5048b4e4223f7ee91bef3644a26a882b63c908ea948af`.
The initial supporting/trigger link IDs were
`15c46107e06a14186fdc7bdd1ef9bd596b1da6d810a5fe18586ca6e9546bace0`
and
`9907a520cafb44883419b567b7cecf99001d971082b7d8ef42e676b566cb8a2c`.

An initial restart preserved its revision-1 hash
`91a28cd63a1a5883f27528c2bd3439a95b2218a4f36d2c7875f0b275edd0bf53`,
membership, link identities, and ledger identities.

### D. Two detection rules for one canonical user — PASS

The same two real Windows detections also produced incident
`6e370316b088797fe34814ce57a3e6b533c6b857ab1e46b78a5e6c9ecf8e3e46`
for `fusion-correlation-user-suspicious-activity` v1, scope fingerprint
`428f0407f50804254e72e5120a813e36bfe7fa750ab9c0b166cced37635afe2f`.
The canonical principal was `kumbesh-desk\user` in the Windows down-level
namespace. Initial membership was exactly two detections from two distinct
rules, with high final severity, confidence 70, `TA0002`, `T1059`, and
`T1059.001`. The initial supporting/trigger links were
`d7aa3a153a946a9e3bc9dc042fd1bc45e6524064fcdd9453c2dc0aae1b9f1173`
and
`3ef4da4cf207e4cb348c607a68320beccd7cd4b4fdd55af0afad2a29e2146513`.

Restart preserved its initial revision-1 hash
`72b6f6fac2835cd460ebd56d177792143068b580ef6377ab6c12a16f5e951034`
and exact two-member set. A later qualifying real input was bounded context
enrichment, not a duplicate incident; its current revision-2 hash is
`2ba6e171d2a602b3d8a30223b6e607e73ccd97bf1ea3a957047297e77d42f9d7`.

### Lifecycle, bounded enrichment, and new episode — PASS

The host incident from scenario B transitioned with two audited operations:

1. `real-step2-ack-host-20260913-1`: `new` to `acknowledged` at
   `06:24:53.875` UTC, resulting revision 2.
2. `real-step2-close-host-20260913-1`: `acknowledged` to `closed` at
   `06:24:54.764` UTC, resulting revision 3.

An attempted `closed` to `acknowledged` transition failed non-zero and created
neither an audit row nor a reopened revision. No-reopen semantics therefore
held.

A third real encoded-PowerShell input occurred at `06:24:06.228` UTC and was
observed at `06:25:09.435`, within the closed incident's bounded enrichment
deadline. Detection
`00fdc9f35db7ebbf76b9528ea8601f8e0e2bb17e88e876fac7cc570dfaeea322`
and source event
`bd6544712f3f3ea2abf9b783db985a5152d6b7ee9ba99cc0e89c41a8d96ed97c`
were added through context link
`eae30084ddf0141241141b1fdb8af68b4bcb517a335fc000bb6d02dca74e30dd`.
The incident remained closed, reached revision 4, retained its immutable
episode interval, and had final hash
`23cdb63269e4fcbe82fc4f00b82a998d7853e5c8de697b770edbf838a826d3b2`.
An identical physical replay left one logical source input, one logical link,
and the same incident state after restart.

Activity after the closed incident's allowed boundary created a new
deterministic episode instead of reopening or changing it. New encoded and
command-shell detections
`276e2eeb1b252c96bdd375c5b382e3c07f549f5cd5891cd677b93e805d2df33c`
and
`466edf81ac3f51205f2441925fdc7452ee0782bc5daa46b51aac4e68ad58ac69`
produced incident
`326c50ec9c08ffd85967da761944201842e5dc07dbbed0ba01636885f317e4ac`.
Its exact two links were
`7fde8c53dc369d8b90624b7ec329b5bae313a1f3c89e70b1e5534a508ad7ca94`
and
`a9432bc4e9619d013a86a6429f179105930ed626cdd42de2ca19077774909860`;
its state hash was
`77a64bf8017eb4f223425672d249f6f59b92c1219b7a752fa9a76623ec99fd0c`.
The original closed incident remained unchanged. Restart preserved both
deterministic incident identities and their disjoint memberships.

### C. Controlled Suricata and endpoint correlation — PASS

The clean acceptance interval began at `2026-09-13 09:32:57.561` UTC. Its
three real inputs, all inside one inclusive ten-minute occurrence window, were:

1. Suricata observed an ICMP packet at `09:33:03.792` from
   `192.168.186.129` to the temporary endpoint alias `192.168.186.10` under
   local SID `9000001`, signature
   `FUSION TEST v0.6 Controlled Cross-Source Acceptance`. Detection ID:
   `833caba8de0df76d4e6bcdc96122a4aa4d5df3b31dd9fa59710aaaea5343be53`;
   source event UID:
   `3862811c6fc50e6023782d5e7d9f3e82f3ab2c0e1866c87221c6f670cf72d9bc`.
2. A harmless encoded PowerShell process occurred on `Kumbesh-Desk` at
   `09:33:16.098`. Its `fusion-windows-encoded-powershell` detection ID was
   `3e803f2c503e5fc929d8b17c05ffb674afedab775539f8a2da2063cfc50d8f4d`,
   source event UID was
   `28e3e127b41a798d7221d93a85cfa463252b07d86cfe17f81137da58e6342363`,
   and MITRE technique was `T1059.001`.
3. Initiated Sysmon Event ID 3 ownership event
   `de27d828e46d04111a6d16c3ba37b59395dd4a71789361335aa94352165cda8e`
   occurred at `09:33:29.139` and was observed at `09:33:31.937`. It proved
   host `Kumbesh-Desk` owned local address `192.168.186.10` for a connection to
   `192.168.186.129:22`.

The resulting incident was:

- incident ID:
  `b8a09e22a99476f49b41c4601d2b1c0b4fae9cf9f8fe8948beb5bb4a0842db6a`;
- rule: `fusion-correlation-suricata-endpoint` v1; scope fingerprint:
  `ec3474ea5fc66066b3ee8118154cc661140bfbe80d0f9652ced95ed98b6a11bb`;
- type/status/severity/confidence:
  `cross_source_network_endpoint` / `new` / `high` / `85`;
- MITRE ATT&CK: `TA0002` / `T1059.001`;
- canonical group: shared IP `192.168.186.10`, host `kumbesh-desk`;
- exact membership: three inputs, two detections, one event, two distinct
  detection rules, and two source families;
- anchor: ownership event
  `de27d828e46d04111a6d16c3ba37b59395dd4a71789361335aa94352165cda8e`;
- episode window: `09:33:03.792`–`09:43:03.792` UTC; late-accept boundary:
  `09:58:03.792` UTC; and
- state hash:
  `7d3e0b58c26e6ba7ce224ede3cb14106659e543b9ddf7f6df3bf1e69271ffb8c`.

The Suricata, Windows-detection, and ownership-event link IDs respectively
were:

- `f29db6fbc8dd7d88baa835d2aa78f619ff32e00a0ea4b2b05b93c063b7ba79ac`;
- `aad8cfcc92c3d47e398e62e7ea188746469adf1eb0dd7cc30e5a3f8297b6cbde`;
- `33378d3b1470e4eab502334bbba2e54cf3a016f27bf8cbea39dee8ade5c6362f`.

After replay and correlation-engine restart, the exact raw/current snapshot was
`1/1` incident, `2/2` detection links, `1/1` event link, `1/1` episode state,
and `3/3` evaluation-ledger rows. The persisted schedule was fully drained
(`0/0/0` for unevaluated inputs, scopes with an oldest input, and oldest age),
with zero canonical-time mismatches, integrity failures, logical duplicate
incidents, or logical duplicate links.

#### Explicitly excluded attempt

Incident
`c872ba78968dd4226a2e25540e9e2955308580bb7f111484787b49e1e2eb512c`
is **not acceptance evidence**. An earlier harness attempt allowed an outer
PowerShell wrapper to contain the encoded-command marker and overlapped with
ambient Windows network ownership facts. Its membership was therefore not the
required exact two detections plus one event. Fusion correlated those visible
facts consistently, but this attempt was excluded, was never relabeled as
accepted, and was not substituted for the clean incident above.

### Cleanup and final health — PASS

- Guarded Suricata cleanup removed temporary SID `9000001`, restored the
  original configuration, revalidated with `suricata -T`, and left both
  `suricata` and `fusion-suricata-vector` active.
- The temporary `192.168.186.10` Windows alias was removed. VMnet8 retained
  only `192.168.186.1/24` in `Preferred` state.
- The disposable Linux account and temporary SSH test key were removed.
- All five Fusion containers were healthy after the final correlation restart.
- The Fusion Incidents Grafana dashboard displayed real incident data and
  deterministic timelines without query errors during acceptance.
  Confirmation-aware SQL views remained the authoritative source for identity
  and counts.
- Final schedule backlog was zero, and duplicate-incident and duplicate-link
  queries returned no rows.

The procedure below is retained as the repeatable acceptance contract.

## Required lab state

Before generating activity:

- deploy the v0.6 feature branch in an isolated Compose project and keep any
  retained release volumes untouched;
- confirm `clickhouse`, `vector`, `fusion-detection-engine`,
  `fusion-correlation-engine`, and `grafana` are healthy;
- validate all detection and correlation rules with the repository tooling;
- synchronize the Fusion host, Windows endpoint, Linux endpoint, and Suricata
  sensor clocks;
- confirm the Windows and Linux Vector agents and the Suricata Vector
  integration are healthy and target the same private Fusion receiver;
- keep TCP 8686 and the plaintext syslog ports restricted to the isolated lab;
- select a unique UTC start time and record current incident/link counts; and
- use only naturally generated rows whose `validation_id` is empty. Never
  relabel fixtures or injected JSON as real telemetry.

Set a project name only when the acceptance stack is not the current Compose
project:

```powershell
$env:COMPOSE_PROJECT_NAME = "fusion-v060-acceptance"
docker compose ps
```

The following command opens an authenticated ClickHouse client inside its
container without printing the generated password:

```powershell
docker compose exec clickhouse sh -lc `
  'exec clickhouse-client --user "$CLICKHOUSE_USER" --password "$CLICKHOUSE_PASSWORD"'
```

Use the confirmation-aware `fusion.incidents_current` and
`fusion.incident_timeline` views for acceptance evidence. Do not infer logical
counts from the raw revision or link tables.

Record this baseline before each scenario:

```sql
SELECT
    correlation_rule_id,
    count() AS logical_incidents,
    sum(detection_count) AS detection_links,
    sum(event_count) AS event_links
FROM fusion.incidents_current
GROUP BY correlation_rule_id
ORDER BY correlation_rule_id;
```

## A. SSH failures followed by a successful login

Expected correlation rule:
`fusion-correlation-ssh-bruteforce-success`. Expected incident severity is
`high`, confidence is `90`, and MITRE ATT&CK is `TA0006` / `T1110`.

1. On an owned isolated Linux VM, create one disposable account with a unique
   lab-only password. Do not reuse a personal or production credential.
2. Confirm sshd already permits the controlled password authentication. Do not
   weaken a production SSH server or broadly expose port 22.
3. From one private source IP, invoke this command manually exactly five times,
   entering one intentionally wrong password at each prompt:

   ```powershell
   ssh -o PreferredAuthentications=password `
     -o PubkeyAuthentication=no `
     -o KbdInteractiveAuthentication=no `
     -o NumberOfPasswordPrompts=1 `
     fusion-v06-accept@192.168.186.129
   ```

   This is five deliberate operator actions, not a loop, wordlist, or guessing
   tool. Stop if the target or username is not the disposable account.
4. Within five minutes of the first failure, run the same command once more and
   enter the correct disposable password. Exit the session immediately.
5. Verify five distinct
   `fusion-linux-authentication-failure` detections, one normalized successful
   SSH event, and exactly one new incident. The failures must strictly precede
   the successful-login occurrence time and must share canonical host, user,
   and source IP.
6. Restart only `fusion-correlation-engine`, wait for its health check, and
   confirm that the incident ID, six logical timeline members, and incident
   count do not change.
7. Remove the disposable account and its home directory after preserving only
   the required bounded acceptance evidence.

Negative controls are four failures followed by success, success before the
failures, a different source IP/user/host, and a success more than five minutes
after the first failure. Run negative controls with separate disposable input;
they must not create an incident.

## B. Two distinct suspicious detections on one Windows host

Expected correlation rule:
`fusion-correlation-host-suspicious-activity`. Expected incident base severity
is `medium` and confidence is `70`; linked child severity may raise the final
severity.

On an owned Windows endpoint with Sysmon Event ID 1 enabled, generate two
different harmless process creations in the same interactive user session:

```powershell
$Marker = "FUSION-V06-$([guid]::NewGuid().ToString('N'))"
$PlainText = "Write-Output '$Marker'"
$Encoded = [Convert]::ToBase64String([Text.Encoding]::Unicode.GetBytes($PlainText))
powershell.exe -NoLogo -NoProfile -NonInteractive -EncodedCommand $Encoded | Out-Null
cmd.exe /d /c "rem certutil -urlcache $Marker"
```

The second command is a comment: it does not run `certutil`, fetch a URL, or
write a payload. Its process command line is intentionally shaped to exercise
the existing reviewed test rule. Do not substitute an executable attack
command.

Verify one real `fusion-windows-encoded-powershell` detection and one real
`fusion-windows-suspicious-command-shell` detection with different detection
IDs, the same canonical host, and occurrence times within ten minutes. Exactly
one new `host_suspicious_activity` incident must link the two detections. A
third replay or a second detection from the same rule must not create or
amplify the incident.

Negative controls are the same rule twice, different hosts, blank host, only
low-severity detections, and a second detection more than ten minutes later.

## C. Controlled Suricata and endpoint correlation

Expected correlation rule: `fusion-correlation-suricata-endpoint`. Expected
incident severity is `high`, confidence is `85`, and type is
`cross_source_network_endpoint`.

This test requires three real inputs in the same inclusive ten-minute
occurrence window:

1. a Suricata detection for a packet involving canonical IP X;
2. a real Windows endpoint detection on host H; and
3. an initiated Sysmon Event ID 3 record whose direction-safe local address
   proves that host H owned IP X at that time.

Use only private/local SID `9000001` and a signature beginning `FUSION TEST`.
Follow the guarded preparation and mandatory restoration procedures in
[`demo-v05.md`](demo-v05.md#prepare-the-temporary-suricata-rule). They verify
that the SID does not already exist, back up the original configuration,
validate with `suricata -T` before restart, and refuse unsafe cleanup. Never
modify a community rule.

With the temporary rule loaded, use one ICMP echo between the Windows endpoint
and monitored private sensor IP. Then create one harmless initiated TCP
connection from that same Windows endpoint so Sysmon records its local address;
port 22 may be used only when it is already intentionally exposed inside the
lab:

```powershell
$SensorIp = "192.168.186.129"
ping.exe -n 1 $SensorIp
Test-NetConnection -ComputerName $SensorIp -Port 22 -InformationLevel Quiet
```

Generate the harmless encoded-PowerShell detection from scenario B on the same
endpoint if no qualifying real endpoint detection exists inside this test's
window. Verify EVE contains SID `9000001`, the signature begins `FUSION TEST`,
and the source/destination pair contains the endpoint's canonical IP. Then
verify exactly one cross-source incident with two detection links, one event
link, `source_family_count >= 2`, the expected host, and the same proven IP.

Repeat with fresh episodes, not reused evidence, for network-first,
endpoint-first, and ownership-context-last visibility orders. Also verify the
IPv4-mapped IPv6 form joins to the same canonical address and that simultaneous
multiple legitimate local IP facts remain distinct. These ordering and
normalization cases may require controlled packet delay or service pause, but
must not require editing stored timestamps or relabeling fixture data as real.

Required negative controls are missing ownership, stale ownership, ownership
by another host, no IP intersection, wrong host binding, blank/invalid IP, two
Suricata-only records, and a span over ten minutes. Simple text equality between
a hostname and IP is never sufficient.

Immediately after collecting evidence, run the mandatory cleanup in
[`demo-v05.md`](demo-v05.md#mandatory-suricata-cleanup). Confirm SID `9000001`
is absent, `suricata -T` succeeds, and both `suricata` and
`fusion-suricata-vector` are active. If guarded cleanup refuses a changed file,
stop and investigate; do not delete its restoration directory manually.

## D. Two detection rules for one canonical user

Expected correlation rule:
`fusion-correlation-user-suspicious-activity`. Expected incident base severity
is `medium`, confidence is `70`, and type is
`identity_suspicious_activity`.

The two controlled Windows activities in scenario B can also exercise this
rule when both detections contain the same nonblank canonical Windows principal
and occur within fifteen minutes. Verify exactly one user incident with two
distinct child rule IDs. It may span hosts within the same platform, but it
must not cross platforms or identity namespaces.

Run separate negative controls proving:

- two outputs from the same detection rule do not qualify;
- blank users do not qualify;
- `DOMAIN\alice`, `alice@domain`, and bare `alice` do not alias;
- Windows case-only variants canonicalize together;
- Linux case-only variants remain distinct; and
- inputs fifteen minutes plus one millisecond apart do not qualify.

Do not change endpoint identity data or source timestamps to manufacture a real
result. Cases that cannot be produced safely as real telemetry remain automated
fixture evidence and must be reported as such.

## Evidence queries

Replace the example rule and UTC timestamp with the scenario under review:

```sql
SELECT
    incident_id,
    correlation_rule_id,
    correlation_rule_version,
    incident_type,
    status,
    severity,
    confidence,
    created_at,
    first_seen,
    last_seen,
    primary_host,
    primary_user,
    primary_source_ip,
    primary_destination_ip,
    input_count,
    detection_count,
    event_count,
    source_family_count,
    mitre_tactic_ids,
    mitre_technique_ids,
    episode_anchor_kind,
    episode_anchor_id,
    validation_id
FROM fusion.incidents_current
WHERE correlation_rule_id = 'fusion-correlation-ssh-bruteforce-success'
  AND created_at >= toDateTime64('2026-01-01 00:00:00', 3, 'UTC')
ORDER BY created_at, incident_id;
```

For each returned ID, verify deterministic membership and visible lateness:

```sql
SELECT
    occurred_at,
    observed_at,
    input_kind,
    input_id,
    relationship,
    source_type,
    rule_id,
    rule_name,
    host_name,
    user_name,
    source_ip,
    destination_ip,
    severity,
    mitre_technique_ids,
    summary,
    validation_id
FROM fusion.incident_timeline
WHERE incident_id = '<INCIDENT_ID>'
ORDER BY occurred_at, input_kind, input_id;
```

Require `validation_id = ''` on the incident and every timeline row. Also prove
that detection links resolve to current detections with empty `validation_id`
and event links resolve to normalized source rows with empty `validation_id`.
Record exact counts and IDs without copying raw event JSON, full command lines,
passwords, or unrelated user data into the acceptance report.

After each scenario and again after restart, require a fully drained persisted
schedule snapshot:

```sql
SELECT
    sum(unevaluated_input_count) AS unevaluated_inputs,
    countIf(oldest_unevaluated_observed_at IS NOT NULL) AS scopes_with_oldest,
    max(oldest_unevaluated_age_seconds) AS oldest_unevaluated_age_seconds
FROM fusion.correlation_schedule_state_current;
```

The required result is `0`, `0`, and `0.0`. Also require both duplicate checks
to return zero rows:

```sql
SELECT incident_id, count() AS logical_rows
FROM fusion.incidents_current
GROUP BY incident_id
HAVING logical_rows != 1;

SELECT incident_id, input_kind, input_id, count() AS logical_rows
FROM fusion.incident_timeline
GROUP BY incident_id, input_kind, input_id
HAVING logical_rows != 1;
```

The `Fusion Incidents` dashboard is read-only. Select the incident from
**Current incidents (filtered by last seen)** and visually confirm its detail
and deterministic timeline. Dashboard display is supporting evidence; the
confirmation-aware SQL views remain the count and membership authority.

## Required result record

For every scenario record:

- UTC start/end and lab topology;
- branch/commit, Compose project, image IDs, and ruleset fingerprints;
- source host/user/IP values in their bounded canonical display form;
- detection IDs, event UIDs, incident ID, link IDs, and episode anchor;
- rule ID/version/fingerprint, status, severity, confidence, and MITRE IDs;
- exact logical membership before and after correlation-engine restart;
- final exact unevaluated backlog and oldest-input age;
- container health and relevant bounded logs;
- PASS/FAIL/SKIPPED with a reason; and
- all temporary accounts, keys, rules, and configuration removed afterward.

Do not mark v0.6 real acceptance complete unless all four positive scenarios,
their required negative controls, restart/deduplication, cleanup, dashboard
visual inspection, and the performance gates in
[`v06-acceptance-plan.md`](v06-acceptance-plan.md) have passed with retained
evidence. Do not create a v0.6 tag from this procedure alone.
